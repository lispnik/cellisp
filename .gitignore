# Compiled Lisp artifacts (ASDF builds into ~/.cache; these only appear from a
# manual compile-file in-tree).
*.fasl
*.fas
*.lx64fsl
*.o
*.a

# ocicl package manager: ocicl.csv (the pinned dependency manifest) is tracked;
# ./ocicl is the downloaded source cache, regenerated by `ocicl install'.
ocicl/
/systems/
